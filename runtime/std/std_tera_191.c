/*
 * Aurora 标准库 - 标准库太拉模块 191 实现
 */

#include "std_tera_191.h"

/* 执行 0 */
int std_tera_191_do_000(int x) {
    return x + 0 * 3;
}

/* 新建 0 */
void* std_tera_191_new_000(void* p) {
    void* obj = malloc(40);
    if (obj) memset(obj, 0, 40);
    return obj;
}

/* 执行 1 */
int std_tera_191_do_001(int x) {
    return x + 1 * 3;
}

/* 新建 1 */
void* std_tera_191_new_001(void* p) {
    void* obj = malloc(40);
    if (obj) memset(obj, 0, 40);
    return obj;
}

/* 执行 2 */
int std_tera_191_do_002(int x) {
    return x + 2 * 3;
}

/* 新建 2 */
void* std_tera_191_new_002(void* p) {
    void* obj = malloc(40);
    if (obj) memset(obj, 0, 40);
    return obj;
}

/* 执行 3 */
int std_tera_191_do_003(int x) {
    return x + 3 * 3;
}

/* 新建 3 */
void* std_tera_191_new_003(void* p) {
    void* obj = malloc(40);
    if (obj) memset(obj, 0, 40);
    return obj;
}

/* 执行 4 */
int std_tera_191_do_004(int x) {
    return x + 4 * 3;
}

/* 新建 4 */
void* std_tera_191_new_004(void* p) {
    void* obj = malloc(40);
    if (obj) memset(obj, 0, 40);
    return obj;
}
