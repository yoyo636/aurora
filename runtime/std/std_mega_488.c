/*
 * Aurora 标准库 - 标准库巨量模块 488 实现
 */

#include "std_mega_488.h"

/* 运行 0 */
int std_mega_488_run_000(int x) {
    return x + 0 * 2;
}

/* 新建 0 */
void* std_mega_488_new_000(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 1 */
int std_mega_488_run_001(int x) {
    return x + 1 * 2;
}

/* 新建 1 */
void* std_mega_488_new_001(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 2 */
int std_mega_488_run_002(int x) {
    return x + 2 * 2;
}

/* 新建 2 */
void* std_mega_488_new_002(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 3 */
int std_mega_488_run_003(int x) {
    return x + 3 * 2;
}

/* 新建 3 */
void* std_mega_488_new_003(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 4 */
int std_mega_488_run_004(int x) {
    return x + 4 * 2;
}

/* 新建 4 */
void* std_mega_488_new_004(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 5 */
int std_mega_488_run_005(int x) {
    return x + 5 * 2;
}

/* 新建 5 */
void* std_mega_488_new_005(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 6 */
int std_mega_488_run_006(int x) {
    return x + 6 * 2;
}

/* 新建 6 */
void* std_mega_488_new_006(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}

/* 运行 7 */
int std_mega_488_run_007(int x) {
    return x + 7 * 2;
}

/* 新建 7 */
void* std_mega_488_new_007(void* p) {
    void* obj = malloc(24);
    if (obj) memset(obj, 0, 24);
    return obj;
}
