/*
 * Aurora 标准库 - 标准库伽马模块 776 实现
 */

#include "std_gamma_776.h"

/* 执行 0 */
int std_gamma_776_do_000(int x) {
    return x + 0 * 37;
}

/* 新建 0 */
void* std_gamma_776_new_000(void* p) {
    void* obj = malloc(120);
    if (obj) memset(obj, 0, 120);
    return obj;
}
