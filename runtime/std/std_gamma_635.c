/*
 * Aurora 标准库 - 标准库伽马模块 635 实现
 */

#include "std_gamma_635.h"

/* 执行 0 */
int std_gamma_635_do_000(int x) {
    return x + 0 * 37;
}

/* 新建 0 */
void* std_gamma_635_new_000(void* p) {
    void* obj = malloc(120);
    if (obj) memset(obj, 0, 120);
    return obj;
}
