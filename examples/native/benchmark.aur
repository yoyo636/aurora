fn fib(n) {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    let start = now_ms()
    let result = fib(35)
    let end = now_ms()
    println("fib(35) = {result}")
    println("耗时: {end - start} ms")
}
